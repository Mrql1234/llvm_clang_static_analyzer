数组越界：上界和下界, 下标为整数
除以零
死循环
缓存区读写操作是否越界
    使用strcpy strcat等字符串函数时超过目标缓冲区大小：
        char buffer[10]; strcpy(buffer, "This is a long string"); // 超出 buffer 的大小
是否使用递归导致栈溢出
指针使用前是否进行非空判断
变量使用前是否初始化
是否对浮点数使用等号进行相等比较
是否出现无符号数负值
反三角函数是否判断参数范围
浮点数转整数是否有溢出
是否存在函数内定义大内存的局部变量，导致栈溢出
memcopy函数拷贝，目标地址空间是否足够
memset函数，目标地址空间是否大于要填充的字节数
sqrt函数输入为负数
浮点运算是否使用双精度浮点数
是否使用有符号数和无符号数进行比较 